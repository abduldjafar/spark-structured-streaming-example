# How to use
1. clone this repo and change to branch that you want
2. make sure you have python installed. then create python environment for run this project. for create  environment
    `python3 -m venv .venv`
3. activate your environment with `source .venv/bin/activate`